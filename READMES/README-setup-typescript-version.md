### Setup

0. pre-requisites:
- node.js
- terminal

1. `cd typescript-version`
2. `npm install`
3. `npm run test`
4. exit: `[CTRL][C]`

#### run single test
`npx vitest run __tests__/goban/goban.chain.test.ts`
